# A SEPARATE, deliberately LOWER-PRIVILEGED SCRAM user -- authenticated
# (SASL_SSL succeeds), but authorized for only what ACLs explicitly
# grant it (see AclSetupApp) -- NOT a super.user, unlike admin.
security.protocol=SASL_SSL
sasl.mechanism=SCRAM-SHA-512
sasl.jaas.config=org.apache.kafka.common.security.scram.ScramLoginModule required username="reader" password="reader-secret";
ssl.truststore.location=/etc/kafka/secrets/truststore.p12
ssl.truststore.password=lab-security-changeit
ssl.truststore.type=PKCS12
